# Ethereum Network Configuration
# ⚠️ NEVER commit your actual .env file with real keys!

# Sepolia Testnet RPC (free public node)
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com

# Private Key (NEVER use a key with real funds!)
# Generate a new key: npx hardhat node (copy any test account)
# Or use MetaMask: Account Details > Export Private Key
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# Optional: For contract verification on Etherscan
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Optional: For mainnet deployment (use with caution)
MAINNET_RPC_URL=https://mainnet.infura.io/v3/YOUR-INFURA-PROJECT-ID
